# Gemini Configuration
GEMINI_API_KEY=tu_api_key_aquí
GEMINI_MODE=hybrid  # simulated|hybrid|real

# Server Configuration
PORT=10000
NODE_ENV=development

# Agent Modes (override per agent) - En hybrid, puedes elegir quién es real
AGENT_DIRECTOR_MODE=real
AGENT_ANALITICO_MODE=simulated
AGENT_CREATIVO_MODE=simulated
AGENT_CONTROLADOR_MODE=simulated

# Memory Configuration
SESSION_STORE_PATH=./memory/sessions.json
